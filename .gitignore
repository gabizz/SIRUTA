# Dependency directories
node_modules/
package-lock.json
# Log files
*.log

# Data files
# Fișierul sursă Excel și fișierul JSON generat
siruta.xls
# siruta-data.json should be tracked, so it is NOT in .gitignore

#other temps
.DS_Store